{
  "name": "dev-utils",
  "version": "1.0.0",
  "description": "Internal client-side only developer utilities",
  "main": "index.html",
  "scripts": {
    "start": "http-server -p 8080",
    "build": "tsc && npm run copy-files",
    "copy-files": "cp src/*.html src/*.css dist/",
    "test": "tsx --test tests/*.test.ts"
  },
  "author": "",
  "type": "module",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^22.13.10",
    "http-server": "^14.1.1",
    "typescript": "^5.3.3"
  }
}
